{% set version = "4.6.2" %}
{% set sha256 = "1cd04ea0200b3cfad7dfd8f149aca6ac8ac2209c423c27258cf4a1ded8e366af" %}

package:
  name: mercurial
  version: {{ version }}

source:
  fn: mercurial-{{ version }}.tar.gz
  url: https://mercurial-scm.org/release/mercurial-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  skip: True  # [py3k]
  script: python setup.py install

requirements:
  host:
    - python
    - setuptools  # [win]
  build:
    - {{ compiler('c') }}
  run:
    - python
    - pygments
    - setuptools  # [win]

test:
  commands:
    - hg --version
  imports:
    - hgdemandimport
    - hgext
    - hgext.convert
    - hgext.fsmonitor
    - hgext.fsmonitor.pywatchman
    - hgext.fsmonitor.pywatchman.bser
    - hgext.highlight
    - hgext.largefiles
    - hgext.zeroconf
    - hgext3rd
    - mercurial
    - mercurial.cext
    - mercurial.cffi
    - mercurial.hgweb
    - mercurial.pure

about:
  home: http://mercurial-scm.org
  license: GPL2
  license_file: COPYING
  summary: Source control management tool
  description: |
    Mercurial is a free, distributed source control management tool. It
    efficiently handles projects of any size and offers an easy and intuitive
    interface.
  doc_url: https://www.mercurial-scm.org/guide
  dev_url: https://www.mercurial-scm.org/repo/hg

extra:
  recipe-maintainers:
    - ngoldbaum
    - nehaljwani
